package com.example.touristvisitplace;  // Ensure this matches your package name

import io.flutter.embedding.android.FlutterActivity;

public class MainActivity extends FlutterActivity {
    // No need for custom code here unless required
}
